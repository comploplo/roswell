[package]
name = "roswell-c"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
publish = false

[lib]
name = "roswell_c"
crate-type = ["cdylib", "staticlib"]

[dependencies]
roswell = { path = ".." }
roswell-ros2-compat = { path = "../roswell-ros2-compat" }

[lints]
workspace = true
